#include "DefenderGame.h"

// Compatibility translation unit.
//
// The complete DefenderGame implementation is intentionally compiled from
// gamelogic.cpp, which is part of every existing ZGLOOM platform project.
// This file stays present so newer project files may continue to list it, but
// it must not contain duplicate method definitions.
